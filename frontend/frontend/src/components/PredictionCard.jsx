import { CheckCircle, AlertCircle, ShieldAlert, HeartHandshake } from "lucide-react";

export default function PredictionCard({ result }) {
  if (!result || !result.top3 || result.top3.length === 0) return null;

  const { prediction, top3, precautions } = result;
  const primaryMatch = top3[0];
  const confidenceVal = Number(primaryMatch?.confidence || 0);
  const confidencePercent = Math.round(confidenceVal * 100);

  return (
    <div className="rounded-2xl border border-[var(--border-color)] bg-[var(--bg-surface)] p-6 sm:p-8 shadow-sm space-y-8">
      {/* Primary Result Banner */}
      <div className="space-y-3 border-b border-[var(--border-color)] pb-6">
        <div className="flex items-center gap-2 text-xs font-semibold uppercase tracking-wider text-[var(--primary)]">
          <CheckCircle className="h-4 w-4" />
          <span>Most Likely Condition</span>
        </div>

        <div className="flex flex-col sm:flex-row sm:items-baseline sm:justify-between gap-2">
          <h3 className="text-2xl sm:text-3xl font-extrabold text-[var(--text-main)] capitalize">
            {prediction || primaryMatch?.disease}
          </h3>
          <span className="text-sm font-semibold text-[var(--primary)] bg-[var(--primary-light)] px-3 py-1 rounded-full w-fit">
            {confidencePercent}% match confidence
          </span>
        </div>

        <p className="text-sm text-[var(--text-sub)] leading-relaxed">
          Based on the symptoms you selected, our AI model found the strongest
          correlation with <strong>{prediction || primaryMatch?.disease}</strong>.
        </p>
      </div>

      {/* Other Potential Matches */}
      {top3.length > 1 && (
        <div className="space-y-3">
          <h4 className="text-xs font-bold uppercase tracking-wider text-[var(--text-sub)]">
            Other Possibilities to Consider
          </h4>
          <div className="grid gap-2.5 sm:grid-cols-2">
            {top3.slice(1).map((item, idx) => {
              const pct = Math.round(Number(item.confidence || 0) * 100);
              return (
                <div
                  key={idx}
                  className="flex items-center justify-between rounded-xl border border-[var(--border-color)] bg-[var(--bg-surface-subtle)] p-3.5"
                >
                  <span className="text-sm font-semibold text-[var(--text-main)] capitalize">
                    {item.disease}
                  </span>
                  <span className="text-xs font-bold text-[var(--text-sub)]">
                    {pct}% match
                  </span>
                </div>
              );
            })}
          </div>
        </div>
      )}

      {/* Precautions */}
      {precautions && Object.keys(precautions).length > 0 && (
        <div className="space-y-4">
          <h4 className="text-xs font-bold uppercase tracking-wider text-[var(--text-sub)] flex items-center gap-1.5">
            <HeartHandshake className="h-4 w-4 text-[var(--primary)]" />
            <span>Recommended Steps & Precautions</span>
          </h4>

          <div className="grid gap-3 sm:grid-cols-2">
            {Object.entries(precautions).map(([disease, tips]) => (
              <div
                key={disease}
                className="rounded-xl border border-[var(--border-color)] bg-[var(--bg-surface-subtle)] p-4 space-y-2"
              >
                <span className="text-xs font-bold text-[var(--text-main)] capitalize block">
                  For {disease}:
                </span>
                <ul className="space-y-1.5 text-xs text-[var(--text-sub)] leading-relaxed">
                  {tips.map((tip, i) => (
                    <li key={i} className="flex items-start gap-2">
                      <span className="text-[var(--primary)] font-bold">•</span>
                      <span>{tip}</span>
                    </li>
                  ))}
                </ul>
              </div>
            ))}
          </div>
        </div>
      )}

      {/* Reassurance Disclaimer */}
      <div className="rounded-xl border border-[var(--warning)] bg-[var(--warning-light)] p-4 text-xs text-[var(--text-main)] flex items-start gap-3">
        <ShieldAlert className="h-5 w-5 text-[var(--warning)] shrink-0 mt-0.5" />
        <p className="leading-relaxed">
          <strong>Important Note:</strong> This prediction is generated by an artificial
          intelligence model for informational reference only. If you are feeling severely
          unwell, experiencing chest pain, or having difficulty breathing, please seek
          medical attention immediately.
        </p>
      </div>
    </div>
  );
}
